<IfModule mod_expires.c>
    ExpiresActive On

    # Set caching for images to 1 year
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"

    # Set caching for CSS to 1 month
    ExpiresByType text/css "access plus 1 month"

    # Set caching for JavaScript to 1 month
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType application/x-javascript "access plus 1 month"
    ExpiresByType text/javascript "access plus 1 month"

    # Set caching for fonts to 1 year
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"

    # Cache other static files (HTML can have a lower TTL)
    ExpiresByType text/html "access plus 1 day"
</IfModule>

<IfModule mod_headers.c>
    # Apply Cache-Control for better efficiency
    <FilesMatch "\.(js|css|png|jpg|jpeg|gif|webp|woff|woff2)$">
        Header set Cache-Control "max-age=2592000, public"
    </FilesMatch>

    # For static files that rarely change (e.g., fonts, images)
    <FilesMatch "\.(jpg|jpeg|png|gif|webp|woff2|woff)$">
        Header set Cache-Control "max-age=31536000, public"
    </FilesMatch>
</IfModule>
